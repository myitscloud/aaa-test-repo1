# Continuous Integration Workflow
# Example CI workflow - customize for your project

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Uncomment and customize for your language
      # - name: Set up Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.11'
      #
      # - name: Install dependencies
      #   run: |
      #     pip install flake8 black
      #
      # - name: Run linter
      #   run: |
      #     flake8 src/
      #     black --check src/

      # For Node.js projects
      # - name: Set up Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #
      # - name: Install dependencies
      #   run: npm ci
      #
      # - name: Run linter
      #   run: npm run lint

      - name: Placeholder
        run: echo "Configure linting for your project"

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Uncomment and customize for your language
      # - name: Set up Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.11'
      #
      # - name: Install dependencies
      #   run: pip install -r requirements.txt -r requirements-dev.txt
      #
      # - name: Run tests
      #   run: pytest --cov=src tests/

      # For Node.js projects
      # - name: Set up Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #
      # - name: Install dependencies
      #   run: npm ci
      #
      # - name: Run tests
      #   run: npm test

      - name: Placeholder
        run: echo "Configure testing for your project"

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Uncomment and customize
      # - name: Build
      #   run: npm run build

      - name: Placeholder
        run: echo "Configure build for your project"

  # Optional: Deploy to staging on push to develop
  # deploy-staging:
  #   name: Deploy to Staging
  #   runs-on: ubuntu-latest
  #   needs: build
  #   if: github.ref == 'refs/heads/develop'
  #   steps:
  #     - name: Deploy
  #       run: echo "Deploy to staging"
